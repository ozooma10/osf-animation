#pragma once

// Canonical content-wide include. SceneRegistry.h remains the implementation/header name for
// source compatibility; ContentRegistry is its preferred alias.
#include "Registry/SceneRegistry.h"
