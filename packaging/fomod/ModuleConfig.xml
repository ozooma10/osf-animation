<?xml version="1.0" encoding="utf-8"?>
<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="http://qconsulting.ca/fo3/ModConfig5.0.xsd">
  <moduleName>OSF Animation</moduleName>

  <moduleImage path="fomod\images\header.jpg"/>

  <requiredInstallFiles>
    <folder source="Core" destination="" priority="0"/>
  </requiredInstallFiles>

  <installSteps order="Explicit">
    <installStep name="Content">
      <optionalFileGroups order="Explicit">
        <group name="Animation Library" type="SelectExactlyOne">
          <plugins order="Explicit">
            <plugin name="Install Vanilla + Creature Animation Library">
              <description>Adds Starfield's built-in animations - humans plus 27 creature species - to the scene browser. This is what fills the browser's Library tab; without it the browser only shows any dedicated content packs you install.

Recommended. Pure data (a few MB of JSON), no extra plugin slot used.</description>
              <image path="fomod\images\header.jpg"/>
              <files>
                <folder source="Library" destination="" priority="1"/>
              </files>
              <typeDescriptor>
                <type name="Recommended"/>
              </typeDescriptor>
            </plugin>
            <plugin name="Skip">
              <description>Leaves the browser's Library tab empty. Pick this only if you intend to install dedicated content packs instead.</description>
              <image path="fomod\images\header.jpg"/>
              <typeDescriptor>
                <type name="Optional"/>
              </typeDescriptor>
            </plugin>
          </plugins>
        </group>
        <group name="Immersion Actions" type="SelectExactlyOne">
          <plugins order="Explicit">
            <plugin name="Install Emote Wheel Content">
              <description>Adds the emote pack (wave, what's up, hands on hips, arms crossed, data slate) that the emote wheel shows. Open the wheel with the hotkey you bind in OSF UI's settings menu; emotes can play on the NPC in your crosshair.

Recommended if you want the emote wheel to do anything out of the box.</description>
              <image path="fomod\images\header.jpg"/>
              <files>
                <folder source="Immersion" destination="" priority="1"/>
              </files>
              <typeDescriptor>
                <type name="Recommended"/>
              </typeDescriptor>
            </plugin>
            <plugin name="Skip">
              <description>Installs no emotes. The emote wheel will open but have nothing to show.</description>
              <image path="fomod\images\header.jpg"/>
              <typeDescriptor>
                <type name="Optional"/>
              </typeDescriptor>
            </plugin>
          </plugins>
        </group>
      </optionalFileGroups>
    </installStep>
  </installSteps>
</config>
