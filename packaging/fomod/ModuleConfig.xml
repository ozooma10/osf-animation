<?xml version="1.0" encoding="utf-8"?>
<!--
  FOMOD installer for OSF Animation.
  - Core is always installed (requiredInstallFiles): the SFSE plugin DLL plus the
    OSF / OSFTypes / OSFTest Papyrus scripts + sources.
  The <folder source="Core"> is staged by packaging/build-archive.ps1; destination=""
  puts its internal SFSE/ + Scripts/ layout at the Data root.
-->
<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="http://qconsulting.ca/fo3/ModConfig5.0.xsd">
  <moduleName>OSF Animation</moduleName>

  <requiredInstallFiles>
    <folder source="Core" destination="" priority="0"/>
  </requiredInstallFiles>
</config>
