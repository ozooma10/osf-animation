{
  "name": "@osf/animation-browser",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 8791",
    "build": "npm run typecheck && vite build",
    "verify": "npm test && npm run build",
    "preview": "vite preview --host 127.0.0.1 --port 8791",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "preact": "10.29.7"
  },
  "devDependencies": {
    "@preact/preset-vite": "2.10.2",
    "@types/node": "24.7.2",
    "typescript": "5.9.3",
    "vite": "7.3.6",
    "vitest": "3.2.7"
  }
}
