{
  // Invalid: a node declares BOTH `use` and inline `stages` (exactly one is allowed) -> rejected.
  "schema": 1,
  "id": "osf.u.badxor",
  "entry": "n",
  "nodes": [
    { "id": "n", "use": "osf.u.shared", "stages": [ ["OSF/Anim/x.glb"] ], "loop": { "mode": "hold" } }
  ]
}
